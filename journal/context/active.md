# Active — state owner (where we are, what's next)

This is the single owner of current state + next action. Task checklist → `journal/ops/tasks.md`;
destination → `ROADMAP.md`; history → `journal/log.md` + `ops/sessions/`.

## Now

**✅ CLAUDE-COUNCIL PLUGIN INSTALLED & AUDIT RUN (2026-06-06).**
- `/claude-council:ask` and `/claude-council:status` commands live.
- `council-advisor` agent installed.
- Claude CLI provider added and tested. OpenRouter format mapped for Grok.
- Council ran initial Phase 4b constraints audit (logged in `journal/raw/_inbox/2026-06-06-council-audit-phase-4b.md`).

**✅ BURNER DEMO FULLY WORKING (2026-06-06, `a2e9ec9`).**
- `npm run demo:hero` — single command; starts RAM-backed server, runs hero script, tears down on exit.

## Recommend next

**▶ Record the Hero Demo.** Once a screen recorder is installed (e.g. Kooha / wf-recorder for Niri/Wayland), run `npm run demo:hero`, screen-capture the live run, and post to LinkedIn.

---

**(History — superseded by the actions above.)**
**✅ HERO DEMO HARDENED + SUPERPOWERS INSTALLED (2026-06-06).** Login Continuity (`ensureHumanAuth`), burner profile isolation (`FEATHER_DIR`/`FEATHER_WARM_WORKSPACE`), `@obra/superpowers` extension. LinkedIn debut recording still pending.
