## In Progress

## Active — Phase 3 Gaps

- [ ] **Resolve event bus design decision** ← BLOCKING
  - Option A: EventEmitter on FeatherSession
  - Option B: EventEmitter on SessionManager (tagged by sessionId)
  - Option C: logger-only for now, wire bus when SSE is designed
  - Decision shapes the SSE endpoint — settle before writing code

- [ ] **Gap 5: Dynamic page tracking**
  - Wire context.on("page") in FeatherSession.setContext()
  - Track pages opened after session launch in the page map
  - Pattern: follow DebugCapture which already does this correctly

- [ ] **Gap 3: Tab lifecycle events**
  - Add TAB_CREATED, TAB_CLOSED, TAB_UPDATED to EVENTS catalog
  - Emit at the correct lifecycle points
  - Depends on event bus decision above

- [ ] **Gap 4: toRecord() returns pages: []**
  - Fix misleading contract — page list fetched separately by handlers
  - Either call getPageInfoList() or document the split clearly

- [ ] **Gap 6: PageInfo lacks loadState**
  - Add loadState field so callers can distinguish in-progress navigation from settled page

- [ ] **Gap 7: ProfileLock stale pid**
  - Check whether locking pid is still alive before blocking a workspace launch
  - Stale locks from crashed processes permanently block workspace

- [ ] **Gap 8: Measurement unrecorded**
  - Run benchmark scenario, record actual RAM/CPU numbers for both browser modes
  - Update docs — no speculative claims

## Done

- [x] Phase 3 setup: branch hierarchy (master/dev/ui-playground) established and pushed
- [x] Phase 3 Step 1: API contract cleanup — ISession, ISessionManager interfaces, as-any casts removed
- [x] Phase 3 Step 2: Lifecycle event logging — SESSION_LAUNCH_REQUESTED/CLOSE_REQUESTED/COMPLETED/FAILED
- [x] Critical bug fix: auth handler return after 401 (middleware.ts, commit 834a67a)
- [x] Critical bug fix: atomic lock creation wx flag (profiles/lock.ts, commit 0f7c1ef)
- [x] Critical bug fix: snapshot new Function() → arrow functions (commands/snapshot.ts, commit e5b3079)
- [x] Critical bug fix: registry.delete after cleanup (sessions/manager.ts, commit da432ee)
- [x] Critical bug fix: context.close() 10s timeout (sessions/manager.ts, commit 265c0e8)
- [x] AGENTS.md updated — Phase 3 state, branch rules, change classification, audit reference
- [x] tsconfig DOM lib added — required for Playwright TS arrow function types
- [x] ui-playground/launch.ts — headed Chromium, persistent profile, stealth patches applied
- [x] Phase 2: Documentation suite (COMPLETE, 2026-05-31, commit a39bf6c)
- [x] Phase 2: Headless Core Prototype — Implementation (COMPLETE, 2026-05-31, 129 tests)
- [x] Phase 2 Step 0: Research and plan Phase 2 prototype
- [x] Phase 1 Restart: headless core ADR (ADR-0002)
- [x] Phase 0: workspace setup
