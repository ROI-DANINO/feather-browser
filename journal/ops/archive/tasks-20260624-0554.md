# Current Tasks — v1 Ship & Adopt  (2026-06-24 reorientation)

Checklist only. **Plan of record → `docs/specs/2026-06-24-reorientation-and-roadmap-restructure.md`.**
Front door → `feather.md`; phase index → `ROADMAP.md`; live pointer → `journal/context/active.md`.
Pre-2026-06-24 history (v1 build, Gate A, 5a/5b, the full v2/5d stealth detail) is archived →
`journal/ops/archive/tasks-20260624-reorientation.md`.

**Sequencing (Roi, 2026-06-24 /stop):** harden first → then OSS envelope → a NEW stronger demo LAST.
Get the code solid & trustworthy before showing it off; the existing `npm run demo:hero` works but is basic.

**Next action = Phase 3: a NEW stronger hero demo (LAST).** Phase 2 (OSS envelope) done 2026-06-24
(uncommitted). Phase 1 (Harden) shipped 2026-06-24 (`06cd2e8`). **Stealth is CUT.**

## Phase 0 — Stop the bleed (security)
- [x] **Commit-time secret guard installed** — `.githooks/pre-commit` (prefers gitleaks, else a
      conservative key-shaped-secret scan; tested). 2026-06-24.
- [ ] **(Roi, outside repo)** Rotate/abandon the leaked `roionly9` throwaway account; discard warmed
      cookies tied to it. History NOT scrubbed (Roi's call — throwaway, low blast radius).

## Phase 1 — Harden (security wins + close test gaps) — ✅ DONE 2026-06-24 (`06cd2e8`)
**1a — Security wins (defense-in-depth, from the audit):**
- [x] Write `SECURITY.md` — outsider-first threat model (reconciled to the shipped posture).
- [x] Reject a non-loopback `FEATHER_HOST` unless `FEATHER_ALLOW_NONLOOPBACK=1` (`config.ts resolveHost`).
- [x] MFA bearer-token URL off console (notifier split: token-less `agentUrl` vs token-bearing `humanUrl`).
- [x] `0700`/`0600` perms on run/log/audit/session dirs — **+ the warmed-profile cookie jar itself**
      (`sessions/manager.ts`, caught by the diff-verify) + debug bundles.
- [x] Constant-time auth (`crypto.timingSafeEqual`) + missing/empty/array-header tests; all bearer-secret/
      CSRF compares consolidated through `constantTimeEqual` (`src/util/constant-time.ts`).

**1b — Close the test gaps:**
- [x] Coverage tooling (`@vitest/coverage-v8`) + per-area floor (`capability`/`identity`/`mfa`/`transport`).
- [x] Consolidated `tests/integration/security/` abuse surface + agent-facing schema fuzz + the rule README.
- [x] Warmed-session reuse / persistence / isolation tests (the Cookie-Mine contract).
- [x] `adr-0012` — at-rest plaintext (perms-only) intentional for v1.

**Bonus (this session):** local **MFA resolve-banner** — banner button carries no token; Feather opens
the resolve tab over CDP (token never on the watched page). Closes the MFA-without-Telegram gap.
Design: `docs/specs/2026-06-24-mfa-resolve-banner-design.md`.

## Phase 2 — Make it adoptable (OSS envelope) — ✅ DONE 2026-06-24 (uncommitted)
- [x] **README clone→fail blocker (xs):** added `npx playwright install chromium` to the README +
      `examples/README.md` quickstart.
- [x] 60-second README quickstart + showcase section surfacing the success number (8 PASS/2 PARTIAL,
      both reclassified environmental). `examples/showcase.sh easy` re-verified live → 3/3 PASS.
- [x] `CONTRIBUTING.md` written; CI build badge added to README (`ci.yml`).
- [x] Declared HTTP API **v1** + one-line stability promise (`docs/api-reference.md` Overview); added
      runnable curl for the action (click/type/press), grants, and MFA-challenge endpoints — all three
      curl shapes verified against the live server.

## Phase 3 — Show it off (a NEW, stronger demo) — LAST
- [ ] Design & record a **stronger** hero demo showing Feather's *real* capabilities on a real
      logged-in errand (`wf-recorder` is installed). The current `npm run demo:hero` works but is basic.
- [ ] Add a one-sentence binary "done" line per version row in `feather.md`.

## Later — PARKED (do not start)
- [ ] **Stealth + Behavior-Mine arc (5d) — CUT 2026-06-24.** Static clean across 4 detectors incl. a
      commercial one; behavioral gap = mouse-motion; no real site has blocked Feather. Reopen ONLY if a
      real site blocks a warmed session — and then adopt a maintained engine, don't hand-patch. Full
      detail archived → `journal/ops/archive/tasks-20260624-reorientation.md`.
- [ ] **Visual desktop shell (4b)** + the big stealth-agent / Hybrid vision — deferred (funded-specialist
      territory; not a near-term solo build).

## Shipped (history — detail in archive)
- [x] **Phase 1 Harden (2026-06-24, `06cd2e8`)** — 1a security fixes + 1b test gaps + cookie-jar perms
      (verifier-caught) + local MFA resolve-banner + 2 residuals; SECURITY.md reconciled. Blog 0023.
      Handoff `journal/ops/sessions/every-door-but-the-vault-20260624-0314.md`.
- [x] **Reorientation (2026-06-24)** — audit + research → hybrid target, stealth cut, plan of record,
      secret guard, tracking files reconciled, blog 0022.
- [x] **v1 built & proven** — errand-runner: perception loop, showcase suite, daily-driver, command layer.
- [x] **v2 safety spine** — Gate A (capability gate) + 5a Identity + 5b MFA + await-human handoff; proven
      live through a real Instagram login wall (2026-06-23).
