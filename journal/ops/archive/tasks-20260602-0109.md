## In Progress

## Active — Phase 3 Gaps (priority order)

- [ ] **Gap 4: Fix toRecord() — returns pages:[] even with multiple open tabs**
  - `toRecord()` in `src/sessions/session.ts` always returns `pages: []`
  - Multi-tab sessions (via `openTab()`) now make this actively misleading
  - Check `GetSessionHandler` — it currently calls `session.toRecord()` then merges `getPageInfoList()` separately
  - Fix the contract: either make `toRecord()` async and call `getPageInfoList()`, or ensure the handler always merges correctly and update the ISession contract to reflect split responsibility
  - START HERE

- [ ] **Gap 5: Wire context.on("page") in FeatherSession.setContext()**
  - Pages opened after session launch (via UI, JS, link clicks) are invisible to the API
  - Event bus decision: Option C — log to JSONL only, no bus until SSE is designed
  - In `setContext()`, after the existing `for (const page of context.pages())` loop, add:
    `context.on("page", (page) => { const pageId = newId("page"); this._pages.set(pageId, page); logger.log(TAB_OPENED ...) })`
  - Note: logger not currently available in session.ts — may need to pass it in or handle differently

- [ ] **Gap 3: Tab lifecycle events for dynamically opened pages**
  - TAB_CREATED, TAB_CLOSED, TAB_UPDATED in EVENTS catalog
  - Emit at correct lifecycle points once context.on("page") is wired (depends on Gap 5)
  - TAB_OPENED for explicit API-triggered opens is already done

- [ ] **Gap 6: PageInfo lacks loadState**
  - Add `loadState` field to `PageInfo` interface in `sessions/types.ts`
  - Callers need to distinguish in-progress navigation from settled page
  - Use Playwright's `page.loadState()` or equivalent

- [ ] **Gap 7: ProfileLock stale pid**
  - Check whether locking pid is still alive before throwing PROFILE_LOCKED
  - Stale locks from crashed processes permanently block workspace
  - Read lock file pid, check if process is alive (kill -0 equivalent in Node)

- [ ] **Gap 8: Measurement unrecorded**
  - Run benchmark scenario, record actual RAM/CPU numbers for both browser modes
  - Update docs — no speculative claims

## Done

- [x] Cookie Mine: ADR-0003 written and accepted
- [x] Cookie Mine: ROADMAP.md + AGENTS.md realigned (Phase 4 prerequisite, MCP-compatible hub)
- [x] Cookie Mine: POST /v1/sessions/:sessionId/tabs — tab open pathway implemented
- [x] Cookie Mine: TAB_OPENED event, SessionNotRunningError, FeatherSession.openTab(), SessionManager.openTab(), OpenTabHandler
- [x] Cookie Mine: api-reference.md + PROGRESS.md updated
- [x] Phase 3 Step 1: API contract cleanup — ISession, ISessionManager interfaces, as-any casts removed
- [x] Phase 3 Step 2: Lifecycle event logging — SESSION_LAUNCH_REQUESTED/CLOSE_REQUESTED/COMPLETED/FAILED
- [x] Critical bug fix: auth handler return after 401 (middleware.ts)
- [x] Critical bug fix: atomic lock creation wx flag (profiles/lock.ts)
- [x] Critical bug fix: snapshot new Function() → arrow functions (commands/snapshot.ts)
- [x] Critical bug fix: registry.delete after cleanup (sessions/manager.ts)
- [x] Critical bug fix: context.close() 10s timeout (sessions/manager.ts)
- [x] AGENTS.md updated — Phase 3 state, branch rules, change classification, audit reference
- [x] tsconfig DOM lib added
- [x] ui-playground/launch.ts — headed Chromium, persistent profile, stealth patches applied
- [x] Phase 2: Documentation suite (commit a39bf6c)
- [x] Phase 2: Headless Core Prototype — 129 tests
- [x] Phase 1 Restart: headless core ADR (ADR-0002)
- [x] Phase 0: workspace setup
