## In Progress

## Active — Pre-Phase 4 Code Audit

- [ ] **Optional: Merge dev → master** — Phase 3 stable baseline (do before or after audit)
- [ ] **Create `blog/` directory** — tracked in git, one entry per milestone/decision. First entry: Phase 3→4 research pivot (Linux-only confirmed, Electron eliminated, Tauri candidate, Wayland problem, system Chromium opportunity). Source material: `raw/_inbox/research-phase4-shell-architecture.md` + `raw/_inbox/research-linux-fedora-tools.md`
- [ ] **Code audit** — scan codebase for gaps surfaced by research and Linux-only decision:
  1. Fastify v4 → v5 readiness: check fastify-sse-v2 v5 compat first, then route schemas
     (full JSON required in v5), .listen() form, request.connection, plugin API mixing
  2. Playwright `.type()` deprecated — grep src/ and tests/, replace with `.fill()`
  3. `executablePath` configurability — add env/config option for system Chromium path
     (e.g. `FEATHER_CHROMIUM_PATH`) so RPM Fusion chromium can be used
  4. Linux/Wayland launch flags — check Chromium launch args; add `--ozone-platform` option;
     remove any hardcoded Windows/macOS path assumptions
  5. TAB_UPDATED event — still missing from tab lifecycle
  6. Debug bundle: add `context.tracing.start/stop` to DebugCapture
  7. Playwright version bump: 1.50 → 1.51+ (unlocks `page.screencast()`)
- [ ] **Prioritize and fix audit findings** — implement whatever the audit surfaces

## Done

- [x] Research pass: Playwright updates, Tauri/Electron, Fastify v5, MCP spec, Linux/Fedora tools
- [x] Linux-only confirmed — Electron eliminated, Tauri candidate, Phase 4 architecture TBD
- [x] Research recorded in raw/_inbox/ (6 files)
- [x] SSE event stream: `GET /v1/events` — fastify-sse-v2, bus.ts EventEmitter, sse.ts async-generator handler, 9 lifecycle events, token auth, 5 unit + 5 integration tests; 129 unit + 32 integration passing
- [x] Gap 8: Measurement — both modes benchmarked; results in docs/phase-2-completion.md
- [x] Gap 6: PageInfo loadState — `loadState: string` added to `PageInfo`; populated via `page.evaluate(() => document.readyState)`
- [x] Gap 7: ProfileLock stale pid — `process.kill(pid, 0)` check; ESRCH = stale, unlink; EPERM = alive, throw
- [x] Gap 3: TAB_CREATED, TAB_CLOSED events — wired via context.on("page") + page.on("close") in manager
- [x] Gap 4: toRecord() return type fixed — Omit<SessionRecord, "pages">
- [x] Gap 5: context.on("page") wired in SessionManager.launch() — dynamic pages tracked
- [x] Cookie Mine: ADR-0003 written and accepted
- [x] Cookie Mine: ROADMAP.md + AGENTS.md realigned
- [x] Cookie Mine: POST /v1/sessions/:sessionId/tabs — tab open pathway
- [x] Cookie Mine: TAB_OPENED event, SessionNotRunningError, FeatherSession.openTab(), SessionManager.openTab(), OpenTabHandler
- [x] Phase 3 Step 1: API contract cleanup — ISession, ISessionManager, as-any casts removed
- [x] Phase 3 Step 2: Lifecycle event logging — SESSION_LAUNCH_REQUESTED/CLOSE_REQUESTED/COMPLETED/FAILED
- [x] 5 critical bug fixes (middleware, lock, snapshot, registry, context.close timeout)
- [x] Phase 2: Documentation suite, Headless Core Prototype (129 tests)
- [x] Phase 1 Restart: ADR-0002
- [x] Phase 0: workspace setup
