# Current Tasks — v1 Ship & Adopt  (2026-06-24 reorientation)

Checklist only. **Plan of record → `docs/specs/2026-06-24-reorientation-and-roadmap-restructure.md`.**
Front door → `feather.md`; phase index → `ROADMAP.md`; live pointer → `journal/context/active.md`.
Pre-2026-06-24 history (v1 build, Gate A, 5a/5b, the full v2/5d stealth detail) is archived →
`journal/ops/archive/tasks-20260624-reorientation.md`.

**Next action = Phase 1: record & publish ONE hero demo** of a real logged-in errand with the
already-installed `wf-recorder` (`npm run demo:hero`). **Stealth is CUT — do not restart it.**

## Phase 0 — Stop the bleed (security)
- [x] **Commit-time secret guard installed** — `.githooks/pre-commit` (prefers gitleaks, else a
      conservative key-shaped-secret scan; tested). 2026-06-24.
- [ ] **(Roi, outside repo)** Rotate/abandon the leaked `roionly9` throwaway account; discard warmed
      cookies tied to it. History NOT scrubbed (Roi's call — throwaway, low blast radius).

## Phase 1 — Ship v1 (publish ONE proof + tell the truth)
- [x] Tracking files reconciled to the reorientation plan; phase pointer made truthful; `active.md`
      trimmed; old NOW stack archived. 2026-06-24.
- [ ] **Record & publish ONE hero demo** of a real logged-in errand (`wf-recorder` is installed — the
      old "recorder blocked" excuse was false). Closes v1's own Definition of Done.
- [ ] Add a one-sentence binary "done" line per version row in `feather.md`.

## Phase 2 — Make it adoptable (narrow OSS envelope)
- [ ] 60-second copy-paste README quickstart (clone → start → run one real task).
- [ ] `SECURITY.md` — local-API threat model, outsider-first (loopback bind, bearer token in header
      not cookie, Origin/Host guard, honest residuals).
- [ ] `CONTRIBUTING.md`; CI green with a build badge.
- [ ] Confirm `examples/showcase.sh` works from a clean checkout; publish a reproducible success number
      incl. one logged-in task. Declare the HTTP API **v1** with a one-line stability promise.

## Phase 3 — Harden the moat (session durability + test gaps)
- [ ] Explicit tests for warmed-session reuse / persistence / isolation.
- [ ] Coverage tooling (`@vitest/coverage-v8`) + a floor on `capability`/`identity`/`mfa`/`transport`.
- [ ] Constant-time auth compare (`crypto.timingSafeEqual`) + missing-header / empty / array-header tests.
- [ ] One consolidated `tests/integration/security/` abuse surface + input-fuzz of agent-facing schemas.
- [ ] 3-line ADR: at-rest is "plaintext, perms-only", intentional for v1.
- [ ] Defense-in-depth (from the security audit): reject a non-loopback `FEATHER_HOST` unless opted in;
      stop printing the live MFA token URL to console; `0700` on run/log dirs.

## Later — PARKED (do not start)
- [ ] **Stealth + Behavior-Mine arc (5d) — CUT 2026-06-24.** Static clean across 4 detectors incl. a
      commercial one; behavioral gap = mouse-motion; no real site has blocked Feather. Reopen ONLY if a
      real site blocks a warmed session — and then adopt a maintained engine, don't hand-patch. Full
      detail archived → `journal/ops/archive/tasks-20260624-reorientation.md`.
- [ ] **Visual desktop shell (4b)** + the big stealth-agent / Hybrid vision — deferred (funded-specialist
      territory; not a near-term solo build).

## Shipped (history — detail in archive)
- [x] **v1 built & proven** — errand-runner: perception loop, showcase suite, daily-driver, command layer.
- [x] **v2 safety spine** — Gate A (capability gate) + 5a Identity + 5b MFA + await-human handoff; proven
      live through a real Instagram login wall (2026-06-23).
