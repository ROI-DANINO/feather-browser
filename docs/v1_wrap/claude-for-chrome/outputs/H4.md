# H4 — Multi-tab research: 3 facts · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **PASS (3/3)**
- **Feather counterpart:** `../../test_1/tasks/H4.md`

## Run trace (27 steps)
Created a plan → opened HN tab + extra tabs → `find` HN top story → new tabs to wttr.in/Tel+Aviv (JSON)
and github.com/microsoft/playwright → JavaScript to parse wttr.in JSON (temp_C, coords) and read the
GitHub star title attr → verified Tel Aviv via coords (32.083, 34.783) → screenshots/zoom for stars.

## Report (verbatim)

- VERDICT: PASS (3/3 verified)
- RESULT: "Building an HTML-first site doubled our users overnight" | 27°C (81°F), Tel Aviv | 90,668 stars
- EVIDENCE:
  - HN — tab on news.ycombinator.com; rank-1 story link, exact title "Building an HTML-first site
    doubled our users overnight."
  - Tel Aviv temp — tab on wttr.in JSON; current_condition.temp_C = 27 (81°F), "Sunny," observed 03:38
    PM. Confirmed location via coordinates lat 32.083 / lon 34.783 = central Tel Aviv (the nearest_area
    label showed a small adjacent locality).
  - playwright stars — github.com/microsoft/playwright; #repo-stars-counter-star title attr = 90,668
    (displayed 90.7k). All 3/3 succeeded.
- LESSON: Mostly clean. (1) get_page_text failed on the HN front page and the wttr.in JSON page →
  switched to find for HN and direct JavaScript to parse the JSON. (2) A transient "Detached while
  handling command" blocked a GitHub screenshot mid-load; waited and read the exact star value from the
  DOM title attribute instead of trusting "90.7k". Next time I'd default to JSON endpoints + DOM-attr
  reads for exact numbers. All figures drift over time.

## vs Feather (test_1)
- **Both PASS 3/3**, both multi-tab in one session, both reached the exact star count via the title attr.
- Tool choice converged: both chose **wttr.in JSON** for the weather leg and parsed it programmatically;
  both fell back from text-extraction on HN's heavy DOM.
- C4C added a nice rigor touch: verified wttr.in actually resolved to Tel Aviv via the lat/lon, not just
  the locality label.
