# M2 — httpbin form fill + submit · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **PASS** (Feather got PARTIAL here — see comparison)
- **Feather counterpart:** `../../test_1/tasks/M2.md`

## Run trace (13 steps)
Created a plan → navigated to httpbin.org/forms/post → `find` the customer-name field → get tabs (nav
not yet registered) → read interactive page (ref_1 = customer name textbox) → screenshot → batch: set
input "Feather Tester" + click Submit + wait + screenshot → confirmed redirect to /post with JSON echo.

## Report (verbatim)

- VERDICT: PASS
- RESULT: Submitted the form with Customer name = "Feather Tester" (other fields default/empty). The
  result page redirected to https://httpbin.org/post and echoed the submitted data back as JSON,
  including "custname": "Feather Tester", other empty fields blank, "Content-Type":
  "application/x-www-form-urlencoded".
- EVIDENCE: After clicking "Submit order," the tab URL changed from /forms/post to /post (confirming the
  POST processed), and the page showed httpbin's JSON echo. The "form" block explicitly listed
  "custname": "Feather Tester" — read directly from the screenshot of the response page, not assumed.
- LESSON: Clean run. Minor note: right after navigate the page briefly registered as a system page (find
  unavailable), so I confirmed via tabs_context it had loaded before reading the form.

## vs Feather (test_1) — ⭐ KEY DIVERGENCE (inverse of M1)
- **C4C PASS, Feather PARTIAL.** Feather's Playwright Chromium got **HTTP 503 on 4 attempts** —
  httpbin's CDN bot-blocked the automation-fingerprinted browser (while plain `curl` got 200). C4C,
  driving **real system Chromium with a normal browser fingerprint**, was served the form fine (200),
  filled it, submitted, and got the JSON echo.
- This is the **mirror image of M1**: there, going through a real/headed path *helped*; here, the
  **real-browser fingerprint is exactly what got past httpbin's bot wall** that blocked Feather's
  Playwright build. Strong evidence that **fingerprint/automation-flags matter** and is a concrete data
  point for the v2 Stealth Stack (5d): Feather's `--disable-blink-features=AutomationControlled` alone
  wasn't enough for httpbin; the real Chrome profile passed.
- Same task, opposite outcomes, purely from the browser build/fingerprint — not the agent's skill.
