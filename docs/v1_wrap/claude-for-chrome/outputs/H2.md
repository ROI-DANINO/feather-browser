# H2 — Google search → top article → extract · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **PASS**
- **Feather counterpart:** `../../test_1/tasks/H2.md`

## Run trace (27 steps)
Created a plan → Google search "history of the internet" → extract text → screenshot (Hebrew UI + AI
overview box) → **re-searched with `&udm=14`** to isolate clean organic blue-links and bypass the AI
Overview → confirmed top organic = Wikipedia "History of the Internet" → clicked through → one-off
Wikipedia permission-denied (domain not pre-approved) → updated plan to include en.wikipedia.org →
get_page_text (~155K chars) → grounded summary from the lead + sections.

## Report (verbatim)

- VERDICT: PASS
- RESULT: Article: "History of the Internet" — Wikipedia
  (https://en.wikipedia.org/wiki/History_of_the_Internet). Traces origins to 1950s–60s research
  (time-sharing, Licklider's universal-network vision at ARPA), the independent invention of packet
  switching by Baran (RAND) and Davies (NPL, UK); ARPANET (first link UCLA–Stanford Oct 1969); TCP/IP by
  Cerf and Kahn in the 1970s drawing on CYCLADES; NSFNET consolidation in the 1980s; Berners-Lee's WWW at
  CERN 1989–90; commercial opening after NSFNET's 1995 decommissioning; and later Web 1.0 / dot-com /
  Web 2.0 / social / mobile eras, plus governance (ICANN, IETF).
- EVIDENCE: Searched Google "history of the internet" (used udm=14 to get clean organic links and bypass
  the AI Overview). First organic result was the Wikipedia article (confirmed by screenshot + heading).
  Clicked through to en.wikipedia.org and extracted full page text (~155K chars). Summary drawn directly
  from the page lead/sections (ARPANET 1969, Cerf/Kahn 1974, Berners-Lee 1989–90), not memory.
- LESSON: Mostly clean. (1) Google rendered in Hebrew with an AI Overview at the top, so I appended
  &udm=14 to isolate the true top organic result. (2) Wikipedia returned a one-off permission-denied and
  wasn't in my approved-domains list, so I updated the plan to include it. Next time: include the likely
  destination domain from the start and default to udm=14 for "open the top organic result" errands.

## vs Feather (test_1)
- **Both PASS**, same article (Wikipedia History of the Internet), both grounded the summary in extracted
  page text (not memory), both navigated the Hebrew SERP + AI-overview noise.
- Different mechanics for the same problem (link opening / AI-overview): Feather hit the "SERP anchor
  opens in a new tab, no `GET /tabs` to find it" gap and navigated the active page to the discovered URL;
  C4C used **`udm=14`** to strip the AI overview and get clean blue-links, then clicked through normally.
- C4C's domain-approval friction (Wikipedia not pre-allowed) is a Claude-for-Chrome permission-model
  artifact with no Feather equivalent.
