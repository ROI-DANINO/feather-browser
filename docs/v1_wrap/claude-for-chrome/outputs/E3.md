# E3 — microsoft/playwright stars · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **PASS**
- **Feather counterpart:** `../../test_1/tasks/E3.md`

## Run trace (14 steps)
Created a plan → navigated to github.com/microsoft/playwright → `find` the star element → screenshot
→ read page → JavaScript tool to read the `#repo-stars-counter-star` title/aria-label attributes.

## Report (verbatim)

- VERDICT: PASS
- RESULT: 90,668 stars
- EVIDENCE: On github.com/microsoft/playwright, the star button (#repo-stars-counter-star) displays
  the rounded "90.7k" in the header, but its title attribute holds the exact value "90,668", and its
  aria-label reads "90668 users starred this repository". Both exact-number sources agree, so the
  precise count is 90,668 (not just the rounded "90.7k").
- LESSON: Clean run. The header only shows a rounded figure ("90.7k"), so the key was reading the
  element's title/aria-label attributes rather than the visible text. Star counts change over time, so
  this reflects the value at the moment I checked.

## vs Feather (test_1)
- **Both PASS**, both reached past "90.7k" to the **exact** count via the same `#repo-stars-counter-star`
  element. Feather read 90,664 (@ ~16:55); C4C read 90,668 (@ later) — grew by 4, real drift.
- Convergent strategy: both harnesses independently decided the rounded header was insufficient and
  went to the title/aria attribute. Strong sign the "exact number" instruction lands the same way in
  both.
