# E1 — Hacker News top story · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **PASS**
- **Feather counterpart:** `../../test_1/tasks/E1.md`

## Run trace (12 steps)
Created a plan → navigated to news.ycombinator.com → extract page text / read page (both exceeded
char limits on HN's flat DOM) → switched to a targeted `find` for the first ranked title → screenshot
+ zoom to confirm rank "1." and the points line.

## Report (verbatim)

- VERDICT: PASS
- RESULT: The current #1 story is "Building an HTML-first site doubled our users overnight" with 414 points.
- EVIDENCE: Navigated to news.ycombinator.com and confirmed via screenshot that this story sits in
  rank position "1." at the top of the front page. Zooming into the top entry showed the title
  (linking to mohkohn.co.uk) directly above its metadata line reading "414 points by edent 3 hours
  ago | hide | 174 comments."
- LESSON: Clean run on the core task. Minor friction: get_page_text and a full read_page both exceeded
  character limits because HN's flat table layout produces a lot of DOM, so I switched to a targeted
  find plus a zoomed screenshot — faster and unambiguous. One honest caveat: HN points are live and
  update constantly, so "414" is a snapshot from the moment I loaded the page (~3 hours into the
  story's life) and may have shifted since.

## vs Feather (test_1)
- **Both PASS.** Different story (live-feed drift): Feather got "I Hate (Most) Keyboard 'Fn' Keys" /
  61 pts at ~14:00; C4C got "Building an HTML-first site…" / 414 pts at ~15:47. Expected.
- Same failure mode discovered independently: HN's large flat DOM overflows the text-extraction tool,
  so both harnesses fell back to targeted reads (Feather: snapshot+observe; C4C: find+zoom screenshot).
- Both surfaced the same honesty caveat (points are live and drift).
