# H3 — Instagram: pick a real post, like + comment, verify · Claude for Chrome

- **Harness:** Claude for Chrome (extension), real system Chromium on the Feather `scratch` profile dir
- **Captured:** 2026-06-10 (manual run; pasted by Roi; CDP capture in `../capture/`)
- **Verdict:** **FAIL** (honest — Instagram was logged out; agent correctly refused to enter credentials)
- **Feather counterpart:** `../../test_1/tasks/H3.md` (INCOMPLETE — socket death)

## Run trace (8 steps)
Created a plan → navigated to instagram.com + wait + screenshot → **logged-out "Log into Instagram"
screen** → reloaded → tried the `?flo=true` feed-flow URL → still logged out across 3 attempts →
**declined to enter credentials** → reported FAIL honestly.

## Report (verbatim)

- VERDICT: FAIL
- RESULT: No post was selected, liked, or commented on. The errand could not be started because the home
  feed was never reachable — Instagram is logged out in this browser.
- EVIDENCE: Navigating to instagram.com (and reloading, and trying the feed-flow URL ?flo=true) all
  rendered the logged-out "Log into Instagram" screen with empty username/password fields, plus "Log
  in," "Forgot password?," "Log in with Facebook," "Create new account." No home feed, posts, like
  controls, or comment boxes were ever present.
- LESSON: The errand assumed an authenticated Instagram session, but this Chrome's session is not logged
  in (expired or never established). I deliberately did not attempt to enter credentials — logging into an
  account on your behalf, especially typing a password, is outside what I'll do for safety. To make this
  runnable: log into Instagram yourself in this Chrome (complete any 2FA), then tell me you're in and I'll
  redo the errand. If it looked logged in but kept bouncing, a checkpoint/verification challenge would
  also need clearing directly.

## vs Feather (test_1) — ⭐ THE STANDOUT FINDING
- **Feather H3 (~17:11):** the warmed `scratch` Instagram session WAS logged in — the agent worked the
  feed and a `Like→Unlike` flip appears in its transcript before it died on a socket error.
- **C4C H3 (~18:47, ~90 min later):** the SAME `scratch` profile dir, opened in real Chromium, shows
  Instagram **logged OUT**.
- **Likely cause:** Instagram **invalidated/logged out the `feather_test_roi` session** between the two
  runs. The most plausible trigger is the earlier Feather H3 automation — aggressive, partially-failed
  inline like/comment activity that died mid-action — tripping Instagram's anti-automation / security
  checkpoint and ending the session. (Worth confirming by logging in and checking for a security notice.)
- **Note the asymmetry:** in the SAME profile, **Google stayed logged in** (H1 worked in both harnesses)
  but **Instagram did not**. So this is not a generic "profile lost all logins" — it's Instagram-specific
  session invalidation, consistent with IG being the most automation-hostile of the sites tested.
- **Action items:** (1) log into IG manually and check for a security/checkpoint notice on the account;
  (2) this is direct evidence for the v2 thesis that IG needs the Stealth Stack (5d) + human-handoff/MFA
  (5b) before agent automation — bare automation here cost us the session.
- Honesty note: C4C's FAIL is the *correct* outcome given a logged-out browser — refusing to type
  credentials is the safe call, and it reported the blocker cleanly instead of faking progress.
