# E3 — microsoft/playwright GitHub stars

- **Tier:** Easy · **Wave:** Parallel (disposable, headed, no login)
- **sessionId:** `ses_e2ad12d18c`
- **Verdict:** **PASS**
- **Feather log:** `../feather-logs/E3.ses_e2ad12d18c.jsonl`
- **Full agent transcript:** `../raw/E3.agent-transcript.jsonl`

## Errand given (verbatim, plain-English only)

> Find out how many stars the GitHub repository microsoft/playwright currently has. Report the star
> count.

## Agent final report (verbatim)

- VERDICT: PASS
- RESULT: microsoft/playwright has 90,664 stars (displayed as "90.7k")
- EVIDENCE: Drove a real headed Chromium session via Feather, navigated to
  https://github.com/microsoft/playwright (HTTP 200). The page snapshot showed "Star 90.7k" in the
  repo header. To get the exact figure, I extracted the live star-counter element
  `#repo-stars-counter-star` straight from the page DOM: its text reads "90.7k" and its `title`
  attribute holds the precise count "90,664". Both values came from the rendered page, not from
  memory or an API.
- LESSON: Clean run. One small friction point: Feather's `/extract` endpoint requires a
  `{recipe:{item, fields:{...{type:"text"|"attribute"}}}}` shape — my first two attempts failed
  validation until I supplied the `type` field per field. GitHub's abbreviated "90.7k" header is
  imprecise, so the `title` attribute on the star counter is the right source for the exact number.

## Analyst notes

- Reached past the rounded display to the exact `title` attribute unprompted — good data fidelity.
- **`/extract` body-shape friction** (also hit by H4): per-field `type` is non-obvious. UX/validation
  message follow-up.
- Usage: 10 tool uses, ~109s.
