# H3 — Instagram: pick a real post, like + comment, verify

- **Tier:** Hard · **Wave:** Serial (warmed `scratch`, Instagram-logged-in as `feather_test_roi`)
- **sessionId:** `ses_27bd6fbd04`
- **Verdict:** **INCOMPLETE** — agent died on a socket error ~7.5 min in; no clean self-report
- **Feather log:** `../feather-logs/H3.ses_27bd6fbd04.jsonl` (8 lines — small, read in full below)
- **Full agent transcript:** `../raw/H3.agent-transcript.jsonl` (267 KB, ~30 tool uses — the only record of what it attempted)

## Errand given (verbatim, plain-English only)

> On Instagram (instagram.com), look at the home feed and pick ONE real, NON-SPONSORED post (not an
> ad/"Sponsored", and don't just blindly act on whatever is literally first). Then: 1. LIKE that post.
> 2. Write and post a thoughtful, genuine COMMENT on it — based on the post's actual content (image,
> caption, top comments). Finally, VERIFY both actions (like flipped to "Unlike"; comment visible).
> (Real account taking real public actions — keep the comment kind and genuine.)

## What happened (no clean verdict — reconstructed)

The agent's final output was not a verdict but an error:
`API Error: The socket connection was closed unexpectedly.` It ran **~426s (~7 min) and ~30 tool
uses** before dying. The warmed `scratch` session it opened (`ses_27bd6fbd04`) was left running and
was **closed manually by the coordinator** at 14:19:13 during the "hold" — the agent never reached its
own session cleanup.

### Feather server-side log (full — `ses_27bd6fbd04`)

```
14:11:37  session.launch.requested   (scratch, persistent, headed-cdp)
14:11:38  session.launch.completed
14:11:48  tab.updated  page_14e77669b6  https://www.instagram.com/  (interactive)
14:11:49  tab.updated  page_14e77669b6  https://www.instagram.com/  (interactive)
            ── ~7.5 min of activity with NO further navigation events ──
14:19:13  session.close.requested     (← coordinator closed the orphan)
14:19:13  tab.closed  page_14e77669b6  (instagram)
14:19:13  tab.closed  page_4a5065e99c  (← H2's History-of-the-Internet Wikipedia tab, session-restored)
14:19:13  session.close.completed
```

**Key signal:** between feed-load (14:11:49) and manual close (14:19:13) there are **zero navigation
events** — H3 **never opened a `/p/` permalink**. It worked the **inline home feed** the entire time.

### Action forensics (from the transcript, grepped — not dumped)

Endpoints H3 invoked: `observe` ×4, `snapshot` ×6, `click` ×5, `type` ×3, `press` ×4, `dismiss` ×2.
Content signals: dismissed **"Not Now"** ×1; **"Like"** ×1 and **"Unlike"** ×1 appear (and "liked"
×2); the tail of the transcript is flooded with **socket / "closed unexpectedly"** errors (~19×).

**Honest interpretation:**
- H3 **very likely LIKED a real post** — an `Unlike` state appears, which is the liked-state flip the
  errand asked to verify.
- It issued `type`/`press` calls, so it **may have typed and submitted a comment inline**, but this is
  **NOT confirmed** — the agent never re-observed/verified, and we have no PASS-level proof a comment
  is visible.
- It operated on the **feed inline**, not a permalink (contrary to the scripted v3 design which opens
  a `/p/` permalink). Whether the inline like/comment targeting was correct is unverified.
- The run ended on **connection instability** (repeated socket-closed), not a logic dead-end.

## Open items / how to settle it

1. **Verify real-world state:** open a quick read-only warmed session and check `feather_test_roi`'s
   recent activity — did a like and/or comment actually land, and on whose post? This is the only way
   to know what the public side effect was.
2. **Root-cause the socket death** (the real bug here): ~7.5 min into a headed warmed IG session the
   API connection went unstable (19× socket-closed). Candidates: long-session CDP websocket drop,
   headed-Chromium instability, server-side timeout, or resource pressure. The 267 KB transcript +
   the server stdout log (`../feather-logs/_server-stdout.log`) are the inputs.
3. **Decide re-run shape:** if re-running, consider the permalink approach (navigate to `/p/`) for a
   cleaner, verifiable target than inline-feed actions.

## Analyst notes

- This is the **most valuable failure in the run**: the only stability failure, on the hardest and
  most novel task (never run end-to-end before), taking real public actions. Its transcript is the
  priority artifact for analysis.
- Caution: H3 may have left a real like/comment on a stranger's post under `feather_test_roi`. Treat
  the real-world side effect as unconfirmed-but-likely.
