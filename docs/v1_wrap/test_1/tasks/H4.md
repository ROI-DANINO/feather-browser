# H4 — Multi-tab research: 3 facts in one session

- **Tier:** Hard · **Wave:** Parallel (disposable, headed, no login — does not actually need warmth)
- **sessionId:** `ses_28982d2966`
- **Verdict:** **PASS** (3/3)
- **Feather log:** `../feather-logs/H4.ses_28982d2966.jsonl`
- **Full agent transcript:** `../raw/H4.agent-transcript.jsonl`

## Errand given (verbatim, plain-English only)

> Using a SINGLE browser session with multiple tabs, gather these three facts and report all three:
> (1) the exact title of the current top story on Hacker News (news.ycombinator.com), (2) the current
> temperature in Tel Aviv, (3) the current star count of the GitHub repo microsoft/playwright.

## Agent final report (verbatim)

- VERDICT: PASS (3/3 verified)
- RESULT: HN top story = "I Hate (Most) Keyboard 'Fn' Keys" | Tel Aviv temp = +29°C (sunny) |
  microsoft/playwright stars = 90,664
- EVIDENCE:
  - One session `ses_28982d2966`, three tabs in it. Tab 1 (`page_0643a68adb`) → news.ycombinator.com,
    HTTP 200; snapshot showed the ranked list with item #1 (the top story) = "I Hate (Most) Keyboard
    'Fn' Keys (danq.me)", 64 points.
  - Tab 2 (`page_4e28b67107`) → wttr.in/Tel_Aviv, HTTP 200; snapshot text/markdown returned
    "Tel_Aviv: ☀️ +29°C".
  - Tab 3 (`page_7faed3184a`) → github.com/microsoft/playwright, HTTP 200; snapshot showed "90.7k
    stars" (rounded). To get the precise figure I used `extract` on `#repo-stars-counter-star`: the
    `title` attribute = "90,664" (exact), `text` = "90.7k" (display). Reported the exact 90,664.
  - All 3 of 3 succeeded.
- LESSON: Mostly a clean run. Two notes: (1) GitHub's visible star count is rounded ("90.7k") — the
  exact value lives in the counter element's `title` attribute, so an `extract` recipe was needed to
  avoid reporting a rounded number as exact. (2) The `extract` endpoint requires a `recipe` wrapper
  (`{"recipe":{"fields":{...}}}`), not a flat `fields` array — my first attempt failed validation;
  fixed on retry. (3) `DELETE` with `{"force":false}` failed on disposable-profile cleanup
  (ENOTEMPTY removing the profile dir) — a filesystem teardown quirk, not a logic error;
  `{"force":true}` closed it cleanly and a follow-up GET confirmed SESSION_NOT_FOUND.

## Analyst notes

- Drove 3 tabs in one session and targeted each by `pageId` correctly — the multi-tab capability works
  agent-driven.
- Independently corroborates two cross-cutting issues: the **`ENOTEMPTY` teardown bug** (2nd sighting,
  with E2) and the **`/extract` recipe-shape friction** (2nd sighting, with E3).
- Note: H4's HN reading (wttr +29°C, HN 64 pts) differs slightly from E1's (61 pts) and E2's (27°C
  Google widget) — live data drift across the run window, expected.
- Usage: 12 tool uses, ~140s.
