# M1 — Web search "Feather Browser"

- **Tier:** Medium · **Wave:** Parallel (disposable, headed, no login)
- **sessionId:** `ses_1af299e5dd`
- **Verdict:** **PASS** ⭐ (scripted suite PARTIAL'd this every run)
- **Feather log:** `../feather-logs/M1.ses_1af299e5dd.jsonl`
- **Full agent transcript:** `../raw/M1.agent-transcript.jsonl`

## Errand given (verbatim, plain-English only)

> Search the web for "Feather Browser" and tell me the title of the top result and what it is about.
> Note: this is a COLD, fresh browser profile with no login — search engines may show a consent/bot
> wall. If that happens, that is real and worth reporting; try a reasonable fallback search engine
> before giving up.

## Agent final report (verbatim)

- VERDICT: PASS
- RESULT: Top result title: **"SimplyCEO / Feather Browser"** (hosted on GitLab,
  `gitlab.com/SimplyCEO/Feather-Browser`). What it is about: it's a minimalist open-source web browser
  project — per the page snippet, *"Feather Browser. A quick example of a browser, made in pure ANSI C
  using GTK3 and WebKit2GTK+"* — i.e. a small Linux browser written in pure ANSI C. (Note: this is an
  unrelated GitLab project that happens to share the name; not this repo.) Google's AI overview above
  the result also flagged that "Feather" is ambiguous, listing other unrelated products: Feather the
  iPad 3D-drawing app, Feather Client (Minecraft launcher), Feather Wallet (Monero), and Feather Wiki
  — but the actual top *web result* is the GitLab Feather Browser project.
- EVIDENCE: Drove a fresh disposable headed Feather session to `google.com/search?q=Feather+Browser`.
  No consent/bot wall appeared — the URL did not redirect to a `/sorry` page and real organic results
  rendered. Confirmed two ways: `observe` showed the first organic result element (refs e45/e46) as
  "SimplyCEO / Feather Browser" / GitLab / `https://gitlab.com › SimplyCEO › Feather-...`, and
  `snapshot` (cleaned page text) showed the same result first under "Web results" with the
  ANSI-C/GTK3/WebKit2GTK+ description. Only engine needed: Google (UI localized to Hebrew, results in
  English).
- LESSON: Clean run. The cold profile did not trigger a bot wall — going straight to the `/search?q=`
  URL avoided the homepage consent flow. The page came back in Hebrew (geo/locale default), but
  `observe`+`snapshot` parsed it fine; no fallback engine required.

## Analyst notes

- **Headline result of test_1.** The scripted suite PARTIAL'd M1 on every run (cold-headless Google
  bot wall → DDG fallback also walled). Here, a **headed** profile hitting `/search?q=` directly got
  clean organic results with **no wall**. Two variables differ from the script: headed (vs headless)
  and direct search URL (vs homepage consent). Worth isolating which mattered.
- Correctly flagged the namesake result is unrelated to this repo — didn't overclaim.
- Usage: 8 tool uses, ~87s.
