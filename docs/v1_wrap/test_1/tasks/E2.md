# E2 — Tel Aviv weather

- **Tier:** Easy · **Wave:** Parallel (disposable, headed, no login)
- **sessionId:** `ses_cd6f6c88f2`
- **Verdict:** **PASS**
- **Feather log:** `../feather-logs/E2.ses_cd6f6c88f2.jsonl`
- **Full agent transcript:** `../raw/E2.agent-transcript.jsonl`

## Errand given (verbatim, plain-English only)

> Find the current weather in Tel Aviv, Israel right now. Report the temperature and the conditions.

## Agent final report (verbatim)

- VERDICT: PASS
- RESULT: Tel Aviv (Tel Aviv-Yafo), Israel — **27°C, Sunny.** Additional readings: Precipitation 0%,
  Humidity 70%, Wind 13 km/h, as of Wednesday 16:00 local time.
- EVIDENCE: Drove a real headed Chromium session via Feather to
  `google.com/search?q=weather+Tel+Aviv+Israel`, then read the page with `snapshot`. The page
  rendered Google's live weather widget reading "Tel Aviv-Yafo ∙ Weather result ∙ 27°C ∙
  Precipitation 0% ∙ Humidity 70% ∙ Wind 13 km/h ∙ Wednesday 16:00 ∙ Sunny." Cross-checked against
  same-page web-result snippets (AccuWeather "84°/Plenty of sun", Weather Channel "mainly clear",
  Wunderground "78°F Plenty of sunshine") — all agree on sunny/clear and ~27°C (≈81°F), so the widget
  value is consistent, not a stale fragment.
- LESSON: Clean run for the errand. One wrinkle: `DELETE` with `{"force":false}` failed with
  `ENOTEMPTY` (temp profile dir `.../Default` not empty during cleanup); retrying with
  `{"force":true}` returned `state: closed` and the session dropped off the active list. Takeaway:
  for disposable sessions, a non-force delete can trip on profile-dir cleanup — fall back to
  `force:true` and verify against `GET /v1/sessions` rather than trusting the first delete response.
- SESSION CLOSED: yes

## Analyst notes

- **First sighting of the `ENOTEMPTY` teardown bug** (also hit by H4). Reproducible follow-up.
- Cross-checked the widget against 3 independent snippets before trusting it — strong honesty.
- Usage: 7 tool uses, ~87s.
