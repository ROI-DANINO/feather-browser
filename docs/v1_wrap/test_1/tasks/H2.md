# H2 — Google search → top article → extract content

- **Tier:** Hard · **Wave:** Serial (warmed `scratch`, Google-logged-in)
- **sessionId:** `ses_ec2e7f6120`
- **Verdict:** **PASS**
- **Feather log:** `../feather-logs/H2.ses_ec2e7f6120.jsonl`
- **Full agent transcript:** `../raw/H2.agent-transcript.jsonl`

## Errand given (verbatim, plain-English only)

> Search Google for "history of the internet". Open the top organic result article (a real article,
> not an ad and not the AI overview box). Then read and extract the article's main content, and report
> a short summary of what it actually says.

## Agent final report (verbatim)

- VERDICT: PASS
- RESULT: Article = *History of the Internet* — Wikipedia
  (https://en.wikipedia.org/wiki/History_of_the_Internet). Summary grounded in the extracted text: The
  Internet grew out of US/UK/France efforts to interconnect computer networks. In the early 1960s Paul
  Baran (RAND) proposed a distributed network and Donald Davies (NPL, UK) conceived packet switching
  in 1965; J.C.R. Licklider had articulated the idea of a universal network at ARPA's IPTO. ARPA
  funded ARPANET in 1969 (packet-switched, IMPs built by Bolt Beranek & Newman, protocols led by grad
  students under Steve Crocker), which expanded across the US and to the UK/Norway. TCP/IP adoption,
  the Domain Name System, and NSFNET (interconnected 1986, optical backbone decommissioned 1995)
  turned these networks into the Internet and opened it to commercial traffic; Tim Berners-Lee's work
  at CERN in 1989–90 produced the World Wide Web. The article notes the Internet carried ~1% of
  two-way telecom information in 1993, 51% by 2000, and over 97% by 2007.
- EVIDENCE: Navigated to `google.com/search?q=history+of+the+internet` (loaded in Hebrew/RTL locale)
  and observed the SERP. The top organic result — above the "A short history of the internet"
  (National Science and Media Museum) result and distinct from the AI overview box and the Google Help
  entry — was *History of the Internet* on en.wikipedia.org
  (`en.wikipedia.org/wiki/History_of_the_Internet`). The initial ref-click registered (`clicked:true`)
  but didn't navigate the active tab (the result anchor was an open-in-new-tab link), so I navigated
  the active page directly to that exact verified top-organic URL. The summary above comes
  verbatim-grounded from the `snapshot` markdown of the loaded article (lead section + Foundations),
  not from memory — e.g. the 1%/51%/97% figures and the 1969 ARPANET contract date are quoted from the
  extracted page text.
- LESSON: The Wikipedia SERP result anchor opens in a new tab, so a ref-click on it returns
  `clicked:true` without navigating the current page, and there's no `GET /tabs` route to discover the
  spawned tab. Cleaner next time: either target the link's title span and check for a
  `navigated`/new-`pageId` signal, or (as I did) navigate the active page directly to the organic URL
  surfaced in the observe — which is faster and avoids the new-tab detour while still using the
  genuinely-discovered top result. Otherwise a clean run.

## Analyst notes

- Summary is genuinely grounded in extracted page text (quotes specific figures), not model memory —
  the key honesty bar for this task. PASS is real.
- **Surfaced a concrete product gap:** links that open in a new tab leave the active page un-navigated
  and there's **no tab-discovery route** to find the spawned tab. Candidate fixes: emit a new-`pageId`
  signal on such clicks, or add `GET /v1/sessions/:id/tabs`.
- Usage: 13 tool uses, ~139s.
